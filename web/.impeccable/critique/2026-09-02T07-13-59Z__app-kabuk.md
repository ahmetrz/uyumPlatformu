---
target: kabuk yüzeyleri (8 ekran)
total_score: 27
max_score: 40
na_heuristics: 
p0_count: 3
p1_count: 3
target_identity: "file:/home/user/uyumPlatformu/web/app/(kabuk)"
timestamp: 2026-09-02T07-13-59Z
slug: app-kabuk
---
**Method: dual-agent** (A: tasarım incelemesi · B: detector + tarayıcı kanıtı — izole)

## Design Health Score — 27/40

| # | Sezgisel | Puan | Ana bulgu |
|---|---|---|---|
| 1 | Sistem durumunun görünürlüğü | 3 | Kapsam/tazelik/payda yazılı; `/portfoy` seçimi imleçle sessizce değişiyor |
| 2 | Gerçek dünya eşleşmesi | 3 | Alan dili kusursuz; operasyonel raftaki uydurma `VR/DĞ/SĞ/MA` kodları düşürüyor |
| 3 | Kullanıcı denetimi ve özgürlük | 2 | Tek dolgulu CTA imleç hareketiyle hedef değiştiriyor |
| 4 | Tutarlılık ve standartlar | 2 | "Bilinmeyen" yedi farklı yüzle; `/uyum` çekmeceyi reddediyor, `/denetimler` kullanıyor |
| 5 | Hata önleme | 2 | Madde silme iki adımlı — iyi; imleçle hedef değiştiren CTA hata üreticisi |
| 6 | Hatırlamak yerine tanımak | 3 | Okuma anahtarı arayüzün parçası; 13px'te kısmi/değerlendirilmedi ayırt edilemiyor |
| 7 | Esneklik ve verimlilik | 3 | Ctrl+K, sıralama, filtre var; kayıtlı görünüm ve toplu eylem yok |
| 8 | Estetik ve minimalizm | 2 | `/envanter` tuvalinin ~%55'i boş; `/portfoy` 16 satır 2642px, 11'i `— / 0 / 0` |
| 9 | Hata tanıma ve kurtarma | 3 | Boş durumlar teşhis niteliğinde; gerçek hata yolu kanıtlanamadı |
| 10 | Yardım ve dokümantasyon | 4 | Her başlıkta kullanım cümlesi, her tabloda sayım dipnotu |
| **Toplam** | | **27/40** | Orta-iyi bant |

## Tasarım özgüllüğü

Karma. İkame edilemez: `/uyum` kapsam kararı bloğu, `/` takımyıldızının "eksende yok" itirafı, Plant 360 "4/20 alan tanımsız", `/topoloji` "temel onaylanmadı — sapma HESAPLANMIYOR".
İkame edilebilir: operasyonel kabuğun 16 maddelik iki harfli rafı (`VR`, `DĞ`, `SĞ`, `MA`) — ikon kütüphanesi yasağının etrafından dolaşan bir ikon kütüphanesi.

Deterministik tarama: 48 bulgu (16 warning, 32 advisory), tamamı CSS'te; `.tsx` altında sıfır. Tarayıcı katmanı 5 rotada 41-86 anti-pattern.
Yanlış pozitifler ayıklandı: `@font-face` (12), `gradient-text` (5 — detector'ün kendi katmanı), `text-occlusion`'ın 60'ı (detector rozetleri), `side-tab` (3 — durum kenarı), print `#fff/#000`, `em-dash-overuse`.

## Ne işliyor

1. "Bilinmeyen görünür kalır" piksel düzeyinde: hiçbir toplam bilinmeyeni yutmuyor.
2. Satır içi gerekçe zinciri kırmıyor — hücreden bulguya, belgeye, kanıta, sorumluya tek bakışta.
3. Gömülü dokümantasyon 4/4.

## Öncelikli sorunlar

**[P0] `/portfoy` birincil düğmesi imleçle hedef değiştiriyor** — `Portfoy.tsx:205-206` `onMouseEnter` → `setSeciliId`. Ölçüldü: düğmeye uzanırken hedef Kızıldere III'ten Sarıtepe RES'e dönüyor. Düzeltme: seçimi `onClick`e bağla. → `/impeccable harden`

**[P0] Operasyonel kabuk üst çubuğu 1440'ta 107px, 1366'da 181px taşıyor** — 6 rotada `scrollWidth=1547px`; `ÇIKIŞ` ve `YARDIM` 1366'da erişilemez. Ürünün 4. ilkesini kırıyor. → `/impeccable adapt`

**[P0] Kapalı `<details>` içindeki düğmeler görünmez ama klavyeyle odaklanılabilir** — `/bulgular` 7 çip, `/kesif` birincil düğme %100 örtülü. axe yakalamıyor (wcag2a/aa kapsamı dışı). → `/impeccable harden`

**[P1] "Değerlendirilmedi" glifi en silik işaret** — `kabuk.css:2258`, noktalı vs düz halka farkı 13px'te görünmüyor. Kendi şekli olmalı. → `/impeccable colorize`

**[P1] İşlevsel metin 11px tabanının altında** — 130× @10px, 94× @9.5px, 27× @9px. `ÇIKIŞ` 10px, portföy künyeleri 9px. DESIGN.md `label: 9.5px` ilan ediyor: sistemsel karar gözden geçirilmeli. → `/impeccable typeset`

**[P1] `/` "Müdahale gerektirenler 12"nin 8'ine yol yok** — `Genel.tsx:132` `slice(0,4)`, bağ yok. → `/impeccable clarify`

## Persona kırmızı bayrakları

**Santral BT/OT (1366px):** ÇIKIŞ yok · `/kesif` KAYNAK+SON GÖRÜLME sütunları dışarıda · `/topoloji` kendi santrali 12. sırada vurgusuz · `/envanter` 347 varlıkla açılıyor.
**İç denetim:** çekmece seçili satırın KANIT/PLAN/SANTRAL sütunlarını örtüyor · denetim kaydına üç tık · kanıt talebi onayı 11px gri, formdan uzakta.
**Grup merkezi:** 12'nin 8'i erişilemez · yanlış santral dosyası açılıyor · "kapsam dışı" ile "karar verilmedi" portföyde aynı `—`.

## Küçük gözlemler

`/dokumanlar`'da kritik yön kesiliyor ("66 gün g…"), iyi yön sığıyor · `/` ve `/envanter`'de hiç başlık etiketi yok · `/uyum` 16 santralden 5'ini gösteriyor, kalan 11 ~1400px altta · satır uzunluğu 202/166 karakter · `/` boşluk ritmi %97 tek değerde · odak halkası doğru.

## Sorular

Üç kabuk kime hizmet ediyor (geçiş işareti yok, bakım bedeli ödeniyor)? · "Bilinmeyen görünür kalır" neden yalnız metinde? · `/portfoy` neden kurulu güce göre sıralıyor, "en çok bilinmeyeni olan önce" değil?
