/* Statik demo: sunucu araması yok. */
export type AramaSonucu = { tip: string; id: string; baslik: string; altBilgi: string; yol: string };
export const ara = async (): Promise<AramaSonucu[]> => [];
